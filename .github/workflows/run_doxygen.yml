name: Generate Doc
on: [push]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Doxygen Action
        uses: mattnotmitt/doxygen-action@1.9.1
        with:
         working-directory: 'docs/doxygen'
